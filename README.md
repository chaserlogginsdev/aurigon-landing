# Aurigon Security — Landing Page

Static landing page for Aurigon Security. Single self-contained HTML file
(only external dependency is Google Fonts) — no build step required.

## Deployment

Deployed via Cloudflare Pages, connected to this GitHub repo.
Every push to `main` triggers an automatic redeploy.

Live URL: (add once deployed)
Custom domain: aurigonsecurity.com (add once connected)
